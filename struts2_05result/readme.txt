结果集：
  <result name="" type="">
  	<param name="namespace">
  	<parma name="actionName">
  </result>
  
* dispatcher   action转发到视图组件（JSP,HTML）

* redirect     action重定向到视图组件（JSP,HTML） 

* chain        action转发到action